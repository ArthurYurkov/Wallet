import { AddButton, CancelButton, Container } from './ModalButtons.styled';

export const ModalButtons = () => {
  return (
    <Container>
      <AddButton>ADD</AddButton>
      <CancelButton>CANCEL</CancelButton>
    </Container>
  );
};
